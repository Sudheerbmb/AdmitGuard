{
  "name": "admitguard-automation-suite",
  "version": "1.0.0",
  "description": "End-to-End Playwright Tests for AdmitGuard Extension and Dashboard",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
